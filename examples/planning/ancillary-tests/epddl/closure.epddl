

(define (domain closure)
    (:objects
    )
    
    (:agents
        a
    )
    
  (:predicates  (p) (q)
  )
   
    (:action apply
        :category (ontic)
        :parameters ()
        :precondition (True)
        :effect (<{(True)} {(K_a (p))}>)
    )

    (:action check
        :category (ontic)
        :parameters ()
        :precondition (DK_a (p))
        :effect             (<{(True)} {(q)}>)
    )

    (:init (K_a (not (p))))

    (:constraint (True)
    )
    
    (:goal (q))
)

