
(define (problem prob-depth3)

    (:domain corridor)

    (:projection )
    (:depth 3)
    (:task valid_generation)

    (:init-type complete)
    (:init

      (at l1)
      (succ l1 l2)
      (succ l2 l3)
      (succ l3 l4)

    )

    (:goal [c](secret) [b](!secret))

)
